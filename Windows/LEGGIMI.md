# Versione Windows

In questa cartella trovi il programma sorgente in Python adattato per Windows e tutti i file necessari per generare l'eseguibile (`.exe`).
A causa dei limiti di dimensione di GitHub, l'eseguibile pre-compilato non è incluso direttamente.

## 🛠️ Come creare l'eseguibile (.exe)

La generazione dell'eseguibile è completamente automatizzata tramite script.

1. Apri la cartella su Windows.
2. Assicurati di avere `Python` installato.
3. Fai doppio clic sul file `crea_exe_windows.bat`.

Lo script installerà in automatico eventuali dipendenze e si occuperà di creare un pacchetto unico `.exe` eseguibile senza installazione.

---

### 🤖 Generazione Automatica tramite Intelligenza Artificiale

**Nota bene:** Se non vuoi avviare lo script localmente o non hai a disposizione l'ambiente di sviluppo per compilare, puoi fornire i file presenti in questa cartella (il file `.py`, `.bat`, `.spec` e `requirements.txt`) o l'URL a questa repo direttamente in pasto a un'Intelligenza Artificiale come **Claude** o **Antigravity**.

Chiedi all'AI: *"Usa questi file per generarmi l'eseguibile .exe del programma per Windows"*, e l'assistente gestirà automaticamente l'ambiente di esecuzione e ti farà scaricare il file `.exe` finale già pronto all'uso!
